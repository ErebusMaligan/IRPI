apply plugin: 'java'

dependencies {
	compile fileTree( dir: "../libs", include: ['JUtils.jar','ModuleAppFramework.jar', 'TerminalWindow.jar'] )
}

ext {
	includeLibs = true
	copyLibs = true
	deployedProjDir = "deployed/Tools"
	deployedLibDir = "./"
	libDest = "deployed/Tools/libs"
	jarPathPrefix = "./libs/"
	
	mainClass = "irpi.app.IRPI"
	iconFile = 'ir_icon.ico'
}

apply from: '../gradle/utilapp.gradle'